[project]
name = "asic-puzzle-tools"
version = "0.1.0"
description = "Warmup-validated GDS inventory, screenshots, and netlist extraction"
requires-python = ">=3.12"
dependencies = [
    "klayout>=0.30.10",
    "matplotlib>=3.11.1",
]
